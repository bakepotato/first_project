name: Action Sample

on:
  push:
    branchs:
      - develop
      - master
  workflow_dispatch:

jobs:
  print-hello-world:
    runs-on: ubunth-latest
    steps:
      - name: execute echo command
        run: echo "hello world!!!"